// Local supabase shim — lightweight stub for development
// Replace this file with the official supabase.min.js for full functionality.
(function(window){
  if (window.supabase && typeof window.supabase.createClient === 'function') return;

  window.supabase = {
    createClient: function(url, key){
      console.warn('Using local supabase shim. Replace js/vendor/supabase.min.js with the official library for full functionality.');
      // minimal client object — add methods as needed for local testing
      return {
        _url: url,
        _key: key,
        // basic auth placeholder
        auth: {
          signInWithOtp: function(){ return Promise.resolve({ data: null, error: null }); },
          verifyOtp: function(){ return Promise.resolve({ data: null, error: null }); }
        },
        // simple query placeholder
        from: function(){ return { select: function(){ return Promise.resolve({ data: [], error: null }); } }; },
        // placeholder for storage or other features
      };
    }
  };
})(window);
